name: "Manual Action"
on:
  workflow_dispatch:
    inputs:
      cond:
        description: "condition"
        required: true
        type: string
jobs:
  validation:
    name: Validation
    if: github.event.inputs.env_name == 'run'
    runs-on: ubuntu-22.04
    steps:
      - name: echo
        run: |
          echo "validated! go to next job"
  hello:
    name: Hello
    runs-on: ubuntu-22.04
    needs: [validation]
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: Hello
        run: |
          echo "Hello, GitHub Actions!!"
